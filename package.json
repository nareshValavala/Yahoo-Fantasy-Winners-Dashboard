{
  "name": "yahoo-fantasy-tracker",
  "version": "1.0.0",
  "description": "Yahoo Fantasy NFL League Tracker",
  "main": "index.html",
  "scripts": {
    "start": "vercel dev",
    "build": "echo 'Static site - no build needed'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "querystring": "^0.2.1",
    "xml2js": "^0.5.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
